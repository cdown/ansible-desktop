---

boot_mode: null
filesystem: null
root_on_ssd: false  # Don't try to TRIM from WSL
compile_server: true
desktop: true  # While we don't use X/dwm, we do use other "desktop" utils
xorg: true
systemd_init: false
server: false
