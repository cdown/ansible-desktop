l4d2: "{{ true if inventory_hostname.startswith('game') else false }}"
nginx: "{{ true if inventory_hostname.startswith('file') else false }}"
rpi: "{{ true if inventory_hostname == 'raspberrypi' else false }}"

create_ap_version: 0.4.6
cap_dir: /opt/create_ap
cap_tgz: "{{ cap_dir }}/{{ create_ap_version }}.tar.gz"
